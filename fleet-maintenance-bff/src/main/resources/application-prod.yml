fleet:
  dynamodb:
    endpoint:
    table-name: ${DYNAMODB_TABLE_NAME}
    outbox-table-name: ${DYNAMODB_OUTBOX_TABLE_NAME}
    region: ${AWS_REGION}

spring:
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}

logging:
  level:
    root: WARN
    com.fleet.maintenance: INFO
